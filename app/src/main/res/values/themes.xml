<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 앱 메인 테마 -->
    <style name="Theme.SMSApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_variant</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSurface">@color/surface</item>

        <item name="android:statusBarColor">@color/primary_variant</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Material 3 AlertDialog (둥근 모서리 + 버튼 색) -->
    <style name="DialogTheme.SMSApp" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="shapeAppearanceCornerLarge">@dimen/corner_16</item>
        <item name="buttonBarPositiveButtonStyle">@style/DialogButton.Positive</item>
        <item name="buttonBarNegativeButtonStyle">@style/DialogButton.Negative</item>
    </style>

    <!-- TextInput 기본 -->
    <style name="Widget.SMSApp.TextInput"
        parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusTopStart">12dp</item>
        <item name="boxCornerRadiusTopEnd">12dp</item>
        <item name="boxCornerRadiusBottomStart">12dp</item>
        <item name="boxCornerRadiusBottomEnd">12dp</item>
    </style>

    <!-- 다이얼로그 버튼(보라색–secondary) -->
    <style name="DialogButton.Positive" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@color/secondary</item>
    </style>
    <style name="DialogButton.Negative" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@color/secondary</item>
    </style>
    <style name="Shape.Corner12" parent="">
        <item name="cornerSize">12dp</item>
    </style>
</resources>
